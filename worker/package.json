{
  "name": "arslan-brochure-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that validates brochure requests and sends the two Resend emails.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
